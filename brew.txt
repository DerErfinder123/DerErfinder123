mkdir -p ~/.homebrew

curl -L https://github.com/Homebrew/brew/tarball/main | tar xz --strip 1 -C ~/.homebrew

echo 'export PATH="$HOME/.homebrew/bin:$PATH"' >> ~/.zshrc

source ~/.zshrc

chmod -R go-w ~/.homebrew


brew install php



mkdir -p ~/.homebrew

curl -L https://github.com/Homebrew/brew/tarball/main | tar xz --strip-components=1 -C ~/.homebrew

echo 'eval "$HOME/.homebrew/bin/brew shellenv"' >> ~/.zshrc

source ~/.zshrc

chmod -R go-w ~/.homebrew