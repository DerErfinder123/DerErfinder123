import socket
client = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
server_ip = "192.168.178.131"
server_port = 8000
send = "test"
client.connect((server_ip, server_port))
client.send(send.encode("utf-8"))
while True:
    send = client.send(1024)
    request = request.decode("utf-8")
    print(request)
